import { StyleSheet } from "react-native";

export const widgetMenuStyles = StyleSheet.create({
  button: { opacity: 0.3, marginBottom: 10, marginHorizontal: "auto" },
  text: { textTransform: "uppercase" },
});
