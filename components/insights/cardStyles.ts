import { StyleSheet } from "react-native";

export const cardStyles = StyleSheet.create({
  container: {
    borderWidth: 1,
    borderRadius: 25,
    flex: 1,
    padding: 30,
    gap: 10,
  },
  title: { fontSize: 30 },
});
