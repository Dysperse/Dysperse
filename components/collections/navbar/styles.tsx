import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  navbarIconButton: {
    flexDirection: "row",
    height: 40,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    width: 50,
    borderRadius: 20,
  },
});
