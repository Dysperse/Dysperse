import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  card: {
    padding: 20,
    height: 80,
    borderRadius: 999,
    alignItems: "center",
    justifyContent: "flex-start",
    gap: 20,
    flex: 1,
    flexDirection: "row",
  },
  cardOutline: {
    borderWidth: 1,
    borderColor: "#000000",
    padding: 20,
    borderRadius: 20,
  },
});
